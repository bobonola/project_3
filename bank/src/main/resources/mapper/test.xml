<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.bank.test.TestMapper">
	<update id="updateSize" parameterType="int">
		update tests
		set size_${size}=size_${size}+1
	</update>
</mapper>
